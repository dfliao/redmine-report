# ======================================
# SYNOLOGY DS920+ 部署 - 環境變數設定
# ======================================

# Redmine 伺服器配置
REDMINE_URL=http://your-redmine.example.com
REDMINE_API_KEY=your_redmine_api_key_here
REDMINE_VERSION=6.0.6
REDMINE_TIMEOUT=30
REDMINE_VERIFY_SSL=true

# Email 配置 (使用 Synology MailPlus Server)
SMTP_HOST=your-synology-nas.local
SMTP_PORT=587
SMTP_USERNAME=GOPEAK@mail.gogopeaks.com
SMTP_PASSWORD=your_mailplus_password
EMAIL_FROM=GOPEAK@mail.gogopeaks.com
EMAIL_USE_TLS=true
EMAIL_USE_SSL=false
EMAIL_TIMEOUT=60
EMAIL_RETRY_ATTEMPTS=3
EMAIL_RETRY_DELAY=5

# 報表配置
REPORT_DAYS=14
TIMEZONE=Asia/Taipei
EMAIL_SUBJECT_TEMPLATE=【Redmine報表】{date} - 議題進度統計
EMAIL_CHARSET=utf-8

# API 服務配置
API_HOST=0.0.0.0
API_PORT=3003
API_PREFIX=/api/v1

# 排程配置 (cron 格式)
# 範例:
# 每日上午 8 點: 0 8 * * *
# 每週一上午 8 點: 0 8 * * 1  
# 每小時執行: 0 * * * *
SCHEDULE_CRON=0 8 * * 1

# 安全配置
DEBUG=false
LOG_LEVEL=INFO
SECRET_KEY=redmine-report-secret-key-2024

# 效能配置 (適用於 Synology NAS)
MAX_WORKERS=2
WORKER_TIMEOUT=300
MEMORY_LIMIT=256M
CPU_LIMIT=0.8

# 快取配置 (選用 - Redis)
# REDIS_URL=redis://redis:6379
CACHE_TTL=3600

# ======================================
# 部署說明:
# 1. 複製此檔案為 .env
# 2. 填入你的 Redmine 和 Email 設定
# 3. 在 Synology Container Manager 中使用 docker-compose.yml 部署
# ======================================